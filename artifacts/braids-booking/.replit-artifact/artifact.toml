kind = "web"
previewPath = "/"
title = "Braids Center Booking"
version = "1.0.0"
id = "artifacts/braids-booking"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21370

[services.development]
run = "pnpm --filter @workspace/braids-booking run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/braids-booking", "run", "build" ]
publicDir = "artifacts/braids-booking/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21370"
BASE_PATH = "/"
